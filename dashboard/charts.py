"""This script will have the base figures to be plotted and then displayed on the dashboard."""

import pandas as pd
import altair as alt

def plot_temp_over_time(conn, plant_id, date_range):
    """Plots temperature over time for each plant."""
    print("Temperature graph goes here.")


def plot_moisture_over_time(conn, plant_id, date_range):
    """Plots soil moisture over time for each plant."""
    print("Moisture graph goes here.")